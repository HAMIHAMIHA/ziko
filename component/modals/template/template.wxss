page {
  position: relative;
}

.modal-component {
  width: 100vw;
  height: 100%;
  color: var(--blk-);
  text-align: center;
  position: absolute;
  left: 0;
  bottom: 0;
  z-index: 10000;
  pointer-events: all;
}

.modal-component.hide {
  opacity: 0;
  pointer-events: none;
}

.mask {
  width: 100vw;
  height: 100%;
  position: fixed;
  left: 0;
  top: 0;
  background: #000;
  opacity: 0.6;
}

.modal-container {
  width: calc(100% - 60rpx); /* Use minus to prevent overlapping mask tap action */
  border-radius: 12rpx;
  overflow: hidden;
  position: fixed;
  left: 50vw;
  top: calc((100vh - var(--env-) - 50rpx) / 2); /* Make sure rating card is centered */
  transform: translate(-50%, -50%);
  z-index: 10001;
  box-sizing: border-box;
}

.card {
  position: relative;
}

.modal-btn-container .btn {
  width: 100%;
}