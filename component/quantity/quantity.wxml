<view class="quantity">
  <button class="ziko-{{ community }} quantity-change" bindtap="changeAmount" data-action="remove" wx:if="{{ quantity }}">
    <view class="icon icon-minus"></view>
  </button>
  <view class="quantity-amount" wx:if="{{ quantity }}">{{ quantity }}</view>
  <button class="ziko-{{ community }} quantity-change" bindtap="changeAmount" data-action="add">
    <view class="icon icon-plus"></view>
  </button>
</view>